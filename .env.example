# Canonical project: https://piegesepycvipfzjbraz.supabase.co
# Copy to `.env` locally. NEVER commit `.env`, `.env.txt`, or real keys.
EXPO_PUBLIC_SUPABASE_URL=https://piegesepycvipfzjbraz.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key

# 1 = home MVP (core only). 0 = Fetish Labs + leisure + suite (not beta home).
EXPO_PUBLIC_MVP=1

# Google OAuth: anon key from Supabase Dashboard → Settings → API.
# Do NOT put Google Client Secret here. Enable the Google provider in Supabase
# and add redirect URLs — see docs/GOOGLE_AUTH.md
# Cloud CI/Pages: set EXPO_PUBLIC_SUPABASE_URL + EXPO_PUBLIC_SUPABASE_ANON_KEY as Actions secrets.
