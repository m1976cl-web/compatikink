import { ACTIVITIES, getAllActivities, getActivityById } from '../data/activities';
import { MOOD_LABELS, ActivityMood, Activity } from '../types';

console.log('Testing node execution, total activities:', ACTIVITIES.length);
